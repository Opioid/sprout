sudo: required
language: cpp

install:
 - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
 - sudo apt-get update -qq
 - sudo apt-get -y install g++-4.9

script:
 - mkdir build
 - cd build
 - qmake --version
 - qmake ../source "QMAKE_CXX=g++-4.9"
 - make