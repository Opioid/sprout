sudo: required
language: cpp

install:
 - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
 - sudo apt-get update
 - sudo apt-get -y install g++-4.9

script:
 - mkdir build
 - cd build
 - qmake ../source "QMAKE_CXX=g++-49"
 - make